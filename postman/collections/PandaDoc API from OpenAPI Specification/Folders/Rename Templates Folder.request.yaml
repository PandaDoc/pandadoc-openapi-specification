$kind: http-request
description: |
  Rename a templates folder.

  For the full list of folder operations and their limitations, see [Organize Templates and Folders](https://developers.pandadoc.com/docs/organize-folders).
url: "{{baseUrl}}/public/v1/templates/folders/:id"
method: PUT
headers:
  Content-Type: application/json
  Accept: application/json
pathVariables:
  - key: id
    value: Nq8htXxFssmhRxAPSP4SBP
    description: The UUID of the folder which you are renaming.
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: json
  content: |-
    {
      "name": "New Folder Name"
    }
  $type: |-
    {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "example": "Another template folder",
          "description": "Provide a new name for the folder."
        }
      },
      "required": [
        "name"
      ]
    }
examples: ./.resources/Rename Templates Folder.resources/examples
order: 6000
