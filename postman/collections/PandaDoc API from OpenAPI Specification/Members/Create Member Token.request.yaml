$kind: http-request
description: |
  > 🚧 
  > 
  > This endpoint is only exposed upon request. Please reach out to your Account Manager to get access.

  > 📘 
  > 
  > Make sure you're using Public API keys generated by Org Admin, otherwise you'll receive a 403 response.
url: "{{baseUrl}}/public/v1/members/:member_id/token"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
pathVariables:
  - key: member_id
    value: radQBiBkU7MBk59NSgaGf5
    description: Member id.
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: json
  content: |-
    {
      "lifetime": 3600
    }
  $type: |-
    {
      "type": "object",
      "properties": {
        "lifetime": {
          "type": "integer",
          "maximum": 86400,
          "description": "Token lifetime in seconds."
        }
      }
    }
examples: ./.resources/Create Member Token.resources/examples
order: 4000
